export enum EUserStatus {
  CREATED = "CREATED",
  BLOCKED = "BLOCKED",
  REJECTED = "REJECTED",
  ACTIVE = "ACTIVE",
}

export enum EUserRole {
  USER = "USER",
  ADMIN = "ADMIN",
  AGENT = "AGENT",
}
